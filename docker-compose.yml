services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stork-code-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
    volumes:
      # Mount node_modules as volume for better performance
      - /app/node_modules
